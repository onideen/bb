schema: http://localhost:1337/graphql

documents:
  - "src/graphql/**/*.graphql"

generates:
  src/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
